// Folha com as funções do JavaScript

function soma(){
  const x = parseFloat(prompt('Digite o valor de x'))
  const y = parseFloat(prompt('Digite o valor de y'))
    let result = x + y
    console.log(result)
};
// ctrl + alt + n     para executar o código.